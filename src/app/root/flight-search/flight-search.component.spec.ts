import {FlightSearchComponent} from './flight-search.component';

describe('FlightSearchComponent', () => {
	it('should create home component', () => {
		let flightSearch = new FlightSearchComponent();
		expect(flightSearch).toBeTruthy();
	});
});
